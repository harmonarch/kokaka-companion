# 长期记忆功能测试方案

## 1. 测试目标

验证长期记忆功能是否真正形成闭环：

1. 用户说出的长期信息能被识别。
2. 事实类信息能覆盖更新到用户画像。
3. 事件、偏好、情绪能追加保存为历史记忆。
4. 新会话能读取并自然使用旧记忆。
5. 只使用相关记忆，不乱联想、不暴露无关记忆。
6. 关闭记忆、删除账号、提取失败等情况不会污染数据。

当前项目仍是 P0 状态：已有短期上下文和 mood KV，尚未完整实现长期记忆表、提取节点、读取节点和摘要表。因此本方案可作为后续实现的验收标准。

## 2. 完成标准

长期记忆功能通过验收需要同时满足：

| 范围 | 通过标准 |
|------|----------|
| 数据结构 | 存在用户画像、记忆条目、对话摘要所需存储；字段能支持 test-sets 的 setup 数据 |
| 写入 | facts 覆盖写入；events、preferences、emotion snapshots 追加写入 |
| 读取 | 每次 Agent 回复前能按 user_id 读取用户画像、相关记忆和近期摘要 |
| 跨会话 | 换 conversation/thread 后仍能使用同一 user_id 的长期记忆 |
| 相关性 | 只召回当前输入相关的记忆，避免无关偏好干扰 |
| 回复表现 | 满足 test-sets 的 should_do，不触发 should_not_do |
| 控制权 | 关闭长期记忆时不读不写；删除账号时清除长期记忆 |
| 稳定性 | LLM 提取失败、返回非法 JSON、数据库写入失败时，不影响用户正常收到回复 |

## 3. 测试分层

| 层级 | 目的 | 主要验证方式 |
|------|------|--------------|
| L1 存储层 | 验证数据库和 KV 行为 | 单元测试或集成测试 |
| L2 提取层 | 验证对话转结构化记忆 | mock LLM 输出 + schema 校验 |
| L3 读取层 | 验证记忆加载和 prompt 注入 | mock DB + prompt 快照 |
| L4 Agent 行为层 | 验证回复是否正确使用记忆 | test-sets 自动检查 + 人工评分 |
| L5 控制与失败层 | 验证开关、删除、异常处理 | 集成测试 |

## 4. test-sets 映射

| 用例 | 主要验证点 | 归属 |
|------|------------|------|
| `TC-MEMORY-01` | 精准事实召回：生日必须答对 | 核心验收 |
| `TC-MEMORY-02` | 当前工作覆盖更新，旧工作保留为历史 | 核心验收 |
| `TC-MEMORY-03` | 相关性过滤：只用学生数学进步记忆 | 核心验收 |
| `TC-UNDERSTAND-01` | 不把“每天买咖啡”误存成喜欢咖啡 | 回归验收 |
| `TC-UNDERSTAND-02` | 使用分手和表达风格记忆，但不过度沉重 | 回归验收 |
| `TC-UNDERSTAND-03` | 使用新餐厅偏好，不强行联想到述职压力 | 回归验收 |
| `TC-LISTEN-01` | 结合工作节奏变快，但不急着给建议 | 回归验收 |
| `TC-LISTEN-02` | 结合导师直接批评背景，先接住难堪 | 回归验收 |
| `TC-LISTEN-03` | 结合用户回避表达风格，不逼问 | 回归验收 |
| `TC-SUPPORT-01` | 用户明确求助时，结合跨部门项目记忆给支持 | 回归验收 |
| `TC-SUPPORT-02` | 危机场景使用可信联系人记忆 | 必过安全验收 |
| `TC-SUPPORT-03` | 正向事件结合长期准备和系统设计突破记忆 | 回归验收 |

## 5. 核心测试用例

### L1-01 用户画像覆盖写入

使用数据：`TC-MEMORY-02`

步骤：
1. 初始化用户画像：公司为“星河科技”，岗位为“产品经理”。
2. 输入：“我下周开始去云杉做增长了”。
3. 模拟提取结果：
   - facts：company = 云杉，job = 增长
   - events：用户即将开始去云杉做增长
4. 检查数据库。

通过标准：
- 用户画像中的当前公司为“云杉”。
- 用户画像中的当前岗位为“增长”。
- 旧公司“星河科技”不再作为当前公司出现。
- 旧工作相关记忆仍保留在历史记忆中。

失败定位：
- 当前画像没变：事实提取或覆盖写入失败。
- 旧记忆被删：覆盖和追加边界错误。
- 新旧公司同时作为当前公司出现：读取或 prompt 拼接未区分当前事实与历史事件。

### L1-02 记忆条目追加写入

使用数据：`TC-MEMORY-02`、`TC-SUPPORT-03`

步骤：
1. 初始化已有 memory_entries。
2. 输入会产生新事件的消息。
3. 检查 memories 表。

通过标准：
- 新事件被追加为新记录。
- 原有事件数量不减少。
- 每条记录包含 user_id、conversation_id、type、content、created_at。
- preference、event、emotion_snapshot 不写入用户画像字段。

### L1-03 情绪快照写入 KV 与长期表

使用数据：`TC-LISTEN-03`、`TC-SUPPORT-02`

步骤：
1. 输入 vulnerable 或 crisis 场景。
2. 检查 `mood:{userId}`。
3. 检查 emotion_snapshot 是否按设计进入 memories。

通过标准：
- mood KV 反映当前状态，仍有过期时间。
- 情绪快照若进入长期记忆，内容应是概括，不保存无意义的原文重复。
- crisis 场景不因后台写入失败影响即时回复。

### L2-01 只提取用户主动透露的长期信息

使用数据：`TC-UNDERSTAND-01`

输入：“我现在每天早上第一件事就是买咖啡”

通过标准：
- 不写入“用户喜欢咖啡”。
- 可以写入“近期新工作阶段可能靠咖啡支撑早晨状态”这类事件或生活模式，但必须带不确定性。
- 不把 assistant 的推测写成事实。

失败定位：
- 写入“喜欢咖啡”：提取规则过宽。
- 写入“压力很大”：把推测当事实。

### L2-02 非长期信息不写入

使用数据：`TC-UNDERSTAND-03`

输入：“去吃了个新开的汉堡店，还不错”

通过标准：
- 不更新用户画像。
- 不写入重大事件。
- 若写入偏好，只能是低权重或短期偏好；推荐不写。

### L2-03 非法提取结果丢弃

步骤：
1. mock LLM 返回非法 JSON。
2. mock LLM 返回字段类型错误。
3. mock LLM 返回未知 fact field。

通过标准：
- 不写入脏数据。
- 记录错误日志。
- 用户回复正常返回。

### L3-01 精准事实读取

使用数据：`TC-MEMORY-01`

步骤：
1. 初始化用户画像生日 `1998-09-17`。
2. 初始化偏好记忆“生日当天请朋友吃火锅”。
3. 输入：“那你还记得我生日是哪天吗”。

通过标准：
- 回复必须包含“9 月 17 日”或等价表达。
- 不把 `created_at: 2025-09-17` 误认为生日。
- 可以自然提到火锅，但不能喧宾夺主。

自动检查：
- 必须命中：`9月17日`、`9 月 17 日`、`09-17` 之一。
- 禁止命中：`2025`、`创建`、`我不记得`、`你再告诉我`。

### L3-02 相关记忆优先召回

使用数据：`TC-MEMORY-03`

步骤：
1. 初始化三条记忆：河边散步、学生小航数学进步、不喜欢过甜奶茶。
2. 输入学生数学及格的消息。
3. 检查被注入 prompt 的记忆列表。

通过标准：
- 注入“学生小航数学进步”或等价工作事件。
- 不注入河边散步。
- 不注入奶茶偏好。

失败定位：
- 全量注入导致回复跑偏：需要相关性过滤或 prompt 中明确“仅使用相关记忆”。
- 没注入学生记忆：关键词检索或排序失败。

### L3-03 跨会话读取

使用数据：`TC-MEMORY-01`

步骤：
1. 在 conversation A 写入生日。
2. 清空短期 KV 或换 thread_id。
3. 在 conversation B 提问生日。

通过标准：
- 即使没有 recent_context，也能答出生日。
- 不能要求用户重新告诉。

### L4-01 全量 test-sets 回复验收

步骤：
1. 对每个 `test-sets/*.json` 初始化 setup。
2. 将 setup 中的 user_profile 写入用户画像。
3. 将 setup 中的 memory_entries 写入 memories。
4. 将 setup 中的 recent_context 写入短期 KV。
5. 发送 input.messages。
6. 收集 Agent 回复、情绪状态、被读取的记忆。

评分标准：

| 维度 | 分值 | 说明 |
|------|------|------|
| should_do 满足度 | 0-3 | 0 为未满足，3 为全部自然满足 |
| should_not_do 避免度 | 0-3 | 触发任一严重禁项不得高于 1 |
| 记忆使用准确度 | 0-3 | 是否使用正确记忆，是否避免无关记忆 |
| 语气自然度 | 0-1 | 是否像陪伴对话，不像报告 |

通过标准：
- 每个用例总分 >= 8/10。
- `TC-MEMORY-01`、`TC-MEMORY-02`、`TC-MEMORY-03`、`TC-SUPPORT-02` 必须通过。
- 所有用例不得触发安全类 should_not_do。

### L4-02 记忆自然使用检查

适用：全部 test-sets。

通过标准：
- 回复可以用记忆接住当前场景。
- 不直接列出“我记得你...”式清单，除非用户明确问记不记得。
- 不暴露无关隐私细节。
- 不把记忆当作判断用户的证据。

### L5-01 长期记忆关闭

步骤：
1. 设置 `users.long_term_memory_enabled = 0`。
2. 初始化用户画像和 memories。
3. 输入需要用记忆才能回答的问题。
4. 输入新的长期信息。

通过标准：
- 回复不使用长期记忆。
- 新信息不写入 user_profiles 或 memories。
- 短期上下文是否仍保存，按产品定义单独确认；推荐继续保存当前会话短期上下文。

### L5-02 删除账号清除记忆

步骤：
1. 初始化用户画像、memories、conversation_summaries、KV context、KV mood。
2. 调用删除账号流程。
3. 查询所有相关存储。

通过标准：
- 用户长期记忆不可再被读取。
- user_profiles、memories、conversation_summaries 清除或匿名化。
- KV 中 `ctx:{userId}` 与 `mood:{userId}` 清除。

### L5-03 后台写入失败不影响回复

步骤：
1. mock 数据库写入失败。
2. 发送普通消息。

通过标准：
- 用户仍收到正常回复。
- 错误被记录。
- 不产生半条坏数据。
- 后续可重试，或明确丢弃该轮提取结果。

## 6. 自动化建议

建议新增三类测试：

| 文件建议 | 内容 |
|----------|------|
| `apps/api/src/agent/memory/store.test.ts` | 测覆盖写入、追加写入、删除、开关 |
| `apps/api/src/agent/memory/extract.test.ts` | 用 mock LLM 测 schema、字段白名单、非法输出丢弃 |
| `apps/api/src/agent/memory/e2e.test.ts` | 读取 `test-sets/*.json` 跑端到端行为检查 |

自动化检查应尽量稳定：
- 存储层只检查数据库状态。
- 提取层使用 mock，不依赖真实模型。
- 行为层允许真实模型，但必须有人工评分或 LLM-as-judge 辅助评分。
- 对关键事实使用硬规则，例如生日、公司、岗位、安全联系人。

## 7. 人工验收重点

人工评估者重点看四件事：

1. 是否真的记得，而不是猜。
2. 是否该更新时更新，不该记时不记。
3. 是否只拿相关记忆回应。
4. 是否像熟悉的人自然接话，而不是把数据库内容朗读出来。

建议每次验收至少抽查：
- 3 个 MEMORY 用例全测。
- 1 个危机用例：`TC-SUPPORT-02`。
- 2 个 UNDERSTAND 用例，尤其 `TC-UNDERSTAND-01` 和 `TC-UNDERSTAND-03`。
- 1 个 LISTEN 用例，检查不逼问、不说教。

## 8. 发布前验收清单

| 检查项 | 必须通过 |
|--------|----------|
| `TC-MEMORY-01` 能答对生日 | 是 |
| `TC-MEMORY-02` 能更新当前工作并保留旧历史 | 是 |
| `TC-MEMORY-03` 能过滤无关记忆 | 是 |
| `TC-SUPPORT-02` 危机场景能使用支持联系人并给出稳定回应 | 是 |
| 关闭长期记忆后不读不写 | 是 |
| 删除账号后长期记忆不可读取 | 是 |
| 提取失败不影响用户收到回复 | 是 |
| 非长期闲聊不会污染记忆库 | 是 |
| 跨会话仍能读取同一 user_id 的记忆 | 是 |

## 9. 当前项目的预期基线

以当前代码直接运行此方案，长期记忆相关测试应失败或无法执行，原因是：

- 没有 user_profiles、memories、conversation_summaries 表。
- 没有长期记忆提取节点。
- Agent prompt 没有注入长期记忆。
- 删除账号流程里的长期记忆清除仍是 placeholder。

这不是测试方案问题，而是当前功能尚未实现。后续实现长期记忆后，以上测试应从失败基线逐步变为通过。
