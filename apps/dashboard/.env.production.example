# 复制本文件为 .env.production 并填入真实值（.env.production 已被 git 忽略）
# 部署时由 scripts/deploy-secrets.mjs 读取并同步到 Worker。

# 访问监控面板的 Basic Auth 密码（用户名 admin 已写在 wrangler.jsonc 的 vars 里）
DASHBOARD_ACCESS_PASSWORD=

# 与 API 侧 MONITORING_API_KEY 保持一致的密钥
MONITORING_API_KEY=
